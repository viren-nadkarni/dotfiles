[user]
    name = Viren Nadkarni
    email = viren@outlook.com

[core]
    editor = vim

[alias]
    aa = add --all
    br = branch
    ci = commit
    cia = commit --amend
    cl = clone --recursive
    co = checkout
    di = diff
    dic = diff --cached
    dih = diff HEAD
    lg = log --oneline --graph --abbrev-commit
    mg = merge
    pu = "!git push -u origin $(git rev-parse --abbrev-ref HEAD)"
    st = status --short --branch

[color]
    ui = true
