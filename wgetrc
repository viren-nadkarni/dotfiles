# append proper extensions to downloaded pages and stylesheets
adjust_extension = on
# follow ftp links from html documents
follow_ftp = on
# force utf-8 as default system encoding
local_encoding = UTF-8
# do not ascend to the parent directory when retrieving recursively
no_parent = on
# ignore the robot exclusion standard
robots = off
